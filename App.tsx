import {View, Text} from 'react-native';
import React from 'react';
import ScrollAnimations from './src/ScrollAnimations/ScrollAnimations';

const App = () => {
  return (
    <View style={{flex: 1}}>
      <ScrollAnimations />
    </View>
  );
};

export default App;
